<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
     <script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>
</head>
<body>
   <div class="w-full min-h-screen bg-gradient-to-br from-cyan-300 via-yellow-200 to-yellow-400  text-black p-10" >
    <div class="w-full flex justify-end">
    <a class="bg-red-300 rounded-md px-3 py-2 text-sm mb-5 inline-block" href="/logout">Logout</a>
    </div>
    <div class="flex items-start gap-3">
    <div class="w-20 h-20  rounded-mb overflow-hidden">
        <img class="w-full h-full object-cover" src="/images/uploads/<%= user.profilepic %>" alt="">
    </div>

   <h3 class="text-3xl"><span class="font-light">Hello</span>,<%= user.name %>!</h3>
   </div>
    <h5 class=" mb-3 mt-4">You can create a post.</h5>
   
    
    <form method="post" action="/post">
        <textarea placeholder="What on your mind?" class=" p-3  block outline-none w-1/3 resize-none bg-transparent border-2 border-zinc-800 rounded-md"name="content"></textarea>
        <input class="px-3 w-40 py-2 text bg-blue-500 block rounded-md mt-2" type="submit" value="Create New Post">
    </form>
    <div class=" posts mt-20">
        <h3 class=" w-30 py-2 bg-blue-500 block rounded-md text-center text-sm text-zinc-800">Your Posts!</h3>
        <div class="postcontainer flex flex-wrap gap-2 mt-5">
            <%user.posts.forEach(function(post){%>

            
            <div class="post w-1/3 p-4 rounded-md border-[1px] border-zinc-800">
                <h4 class="text-blue-500 mb-2 ">@<%= user.username %></h4>
                <p class="text-sm tracking-tight"><%= post.content%> </p>
                <small class="mt-4 inline-block"><%= post.likes.length %> likes</small>
                <div class="bts flex gap-4 mt-2">
                    <a class="text-blue-500"href="/like/<%= post._id %>">
                        <%=post.likes.indexOf(user._id)===-1?"Like" : "Unlike" %>
                    </a>
                    <a class="text-zinc-600"href="/edit/<%=post._id%>">Edit</a>
                </div>
            </div>
            <%})%>
        </div>
    </div>
   </div>
</body>
</html>