<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
     <script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>
</head>
<body class="min-h-screen flex  flex-col  bg-gradient-to-br from-zinc-800 via-black to-zinc-900 text-white p-10">


  <div class="w-full flex justify-end">
    <a href="/logout" class="bg-red-500 hover:bg-red-600 text-white px-3 py-2 rounded-md text-sm transition">Logout</a>
  </div>

  <div class="flex items-center gap-4 mt-5">
    <div class="w-20 h-20 rounded-md overflow-hidden">
      <img class="w-full h-full object-cover" src="/images/uploads/<%= user.profilepic %>" alt="Profile Picture">
    </div>
    <h3 class="text-3xl text-white"><span class="font-light">Hello</span>, <%= user.name %>!</h3>
  </div>
  <h5 class="mb-3 mt-4 text-xl font-semibold text-white">Create a Post</h5>

<form method="post" action="/post" enctype="multipart/form-data"
  class="p-4 bg-white bg-opacity-80 backdrop-blur-sm border border-gray-300 rounded-xl shadow-xl w-full max-w-lg">
  <textarea
    placeholder="What's on your mind?"
    class="w-full p-3 mb-3 rounded-lg border-2 border-gray-400 outline-none resize-none bg-transparent text-gray-900 hover:border-blue-500 focus:border-blue-600 transition"
    name="content"
    rows="3"
  ></textarea>

  <label class="block w-full text-sm text-gray-700 mb-2">Attach a photo (optional)</label>
  <input
    type="file"
    name="image"
    accept="image/*"
    class="block w-full p-2 mb-4 border-2 border-gray-400 rounded-lg cursor-pointer bg-transparent text-gray-900 hover:border-blue-500 focus:border-blue-600 transition"
  />

    <input 
      type="submit" 
      value="Create New Post" 
      class="px-5 py-2 w-40 bg-blue-500 hover:bg-blue-600 text-white rounded-lg font-semibold transition"
    />
  </form>

  <div class="posts mt-10">
    <h3 class="w-full max-w-md py-2 bg-blue-500 rounded-md text-center text-white text-sm">Your Posts!</h3>
    <div class="postcontainer flex flex-wrap gap-4 mt-5 justify-start">
      <% user.posts.forEach(function(post) { %>
        <div class="post w-full sm:w-1/2 md:w-1/3 lg:w-1/4 p-4 rounded-lg border border-gray-300 bg-white bg-opacity-80 backdrop-blur-sm flex flex-col justify-between min-h-[300px]">

          <h4 class="text-blue-500 mb-2">@<%= user.username %></h4>
          <p class="text-sm tracking-tight text-black"><%= post.content %></p>
          <% if (post.image) { %>
      <img src="/images/uploads/<%= post.image %>" alt="Post Image" class="w-full rounded-md mb-2">
    <% } %>
    <div class="bts flex justify-between mt-2 text-sm flex-nowrap mb-2">
      <small class="text-gray-700"><%= post.likes.length %> likes</small>
      <div class="flex gap-4">
    <a class="text-blue-500 hover:underline" href="/like/<%= post._id %>">
      <%= post.likes.indexOf(user._id) === -1 ? "Like" : "Unlike" %>
    </a>
    <a class="text-gray-600 hover:underline" href="/edit/<%= post._id %>">Edit</a>
   <a class="text-red-500 hover:underline" href="/delete/<%= post._id %>">Delete</a>
  </div>
</div>
 </div>
      <% }) %>
    </div>
  </div>

</body>

  
           
</html>